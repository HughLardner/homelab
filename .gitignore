.idea
.terraform*
terraform/secrets.tf
terraform/terraform.tfstate*
terraform/terraform.tfvars
ansible/tmp
*.DS_Store
scripts/.env
env/

# Ansible generated files
ansible/inventory/hosts.yml
ansible/.cache/
ansible/logs/

# Ansible Vault files (unencrypted backups)
ansible/**/vault/*.yaml.bak
ansible/**/vault/*.yml.bak

# Sealed Secrets private keys (if accidentally committed)
sealed-secrets-private-key.pem
sealed-secrets-public-key.pem
sealed-secrets-key-backup.yaml

# Let's Encrypt certificate backup (contains private keys!)
letsencrypt-certs-backup.yaml

# Plain secrets file (NEVER commit this!)
secrets.yml
secrets.yaml
config/secrets.yml
config/secrets.yaml
ansible/secrets.yml
ansible/secrets.yaml

# Cursor IDE files
.cursor/

# Claude Code files (may contain MCP secrets)
.claude/

# Rendered templates (contain secrets)
rendered/
