apiVersion: v2
name: traefik-ingress
description: Traefik Dashboard IngressRoute, Certificate, and Middlewares
type: application
version: 1.0.0
appVersion: "3.0"

# This chart creates the dashboard ingress resources for Traefik
# The main Traefik application is deployed using the official Traefik Helm chart
# with traefik-values.yaml

