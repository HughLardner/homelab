# Pi-hole authentication is handled entirely by Authelia (forward-auth via Traefik).
# WEBPASSWORD is set to an empty string in the Deployment, disabling Pi-hole's own
# login page. No SealedSecret is required.
