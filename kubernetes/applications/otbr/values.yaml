image:
  repository: bnutzer/otbr-tcp
  tag: latest
  pullPolicy: IfNotPresent

priorityClassName: homelab-standard

resources:
  requests:
    cpu: 50m
    memory: 128Mi
  limits:
    cpu: 500m
    memory: 512Mi

# Values loaded from config/homelab.yaml via ArgoCD
services:
  otbr: {}

global: {}
