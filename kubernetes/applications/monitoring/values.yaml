# Default values for monitoring-ingress chart
# These are overridden by config/homelab.yaml when deploying

global:
  domain: silverseekers.org
  cert_issuer: letsencrypt-prod

services:
  grafana:
    domain: grafana.silverseekers.org
    storage_size: 5Gi
    # cert_issuer: letsencrypt-prod  # Falls back to global.cert_issuer if not set

  monitoring:
    storage_class: longhorn
    prometheus_storage_size: 10Gi
    prometheus_retention: 15d
    alertmanager_storage_size: 2Gi
    instability_snapshot_enabled: true
    instability_snapshot_schedule: "*/15 * * * *"  # Reduced from 5min to 15min (66% less API stress)
    instability_snapshot_success_history: 2
    instability_snapshot_failed_history: 2
    etcd_maintenance_enabled: true
    etcd_maintenance_schedule: "0 2 * * 0"  # 2am Sundays
    # Longhorn instance-manager memory leak mitigation
    longhorn_instance_manager_restart_enabled: false  # Enable after Phase 1 stable for 48h
    longhorn_instance_manager_restart_schedule: "7 2,8,14,20 * * *"  # Every 6h at :07
    longhorn_instance_manager_restart_threshold_gb: 1

