---
apiVersion: v1
kind: ConfigMap
metadata:
  name: grafana-dashboard-cloud-edge-dns
  namespace: monitoring
  labels:
    grafana_dashboard: "1"
  annotations:
    grafana_folder: "Infrastructure"
data:
  cloud-edge-dns.json: |-
{{ .Files.Get "dashboards/cloud-edge-dns.json" | nindent 4 }}

